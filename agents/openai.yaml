interface:
  display_name: "Email Manager"
  short_description: "Configurar, ler e enviar emails com seguranca"
  default_prompt: "Use $email-manager to configure IMAP/SMTP with the wizard, review unread emails, and draft replies before asking for approval to send."
