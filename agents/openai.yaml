interface:
  display_name: "Protonskill"
  short_description: "Proton Mail Bridge para ler e enviar email"
  default_prompt: "Use $protonskill to configure Proton Mail Bridge with the wizard, review unread emails, and draft replies before asking for approval to send."
